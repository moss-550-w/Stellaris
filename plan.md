# plan.md —— 项目开发路线图

> 本文件是《游戏级群星运行模拟器》的**可落地开发路线图**，配合 `CLAUDE.md`（项目规范）使用。
> `CLAUDE.md` 定义「是什么、约定什么」；本文件定义「分几步做、每步做什么、做到什么算完成」。

**状态图例：** `[ ]` 未开始 · `[~]` 进行中 · `[x]` 已完成

**说明：** 阶段划分与 `CLAUDE.md` 的「开发阶段与节奏」完全一致，本文档为其可执行展开。阶段零为动工前置（脚手架），`CLAUDE.md` 未单列，是阶段一的准备工作。

---

## 1. 项目总览

**定位：** 以视觉沉浸与沙盒创造为核心的游戏化太空模拟器，给玩家一个物理自洽、视觉震撼、操作自由的「宇宙游乐场」。

**核心体验（五大原则）：** 体验优先 · 物理自洽 · 视觉震撼 · 交互自由 · 性能可控。

**技术栈速查：**

| 领域 | 选型 |
|------|------|
| 构建 | TypeScript + Vite |
| 3D 渲染 | Three.js（WebGL） |
| 后处理 | 自研合并后处理 Shader（单通道） |
| 物理引擎 | 原生 TS（Web Worker），标准模式 Verlet；**不引入 Pyodide** |
| UI | Vue 3 |
| 存储 | LocalStorage + File API |
| 部署 | 纯静态资源，零后端（GitHub Pages / Vercel，可离线） |

**五层松耦合架构（物理与渲染完全解耦）：**

```
┌─────────────────────────────────────┐
│           UI 数据层                  │  Vue 3 面板、存档、设置
├─────────────────────────────────────┤
│          玩法交互层                  │  视角、编辑、成就、沙盒工具
├─────────────────────────────────────┤
│          渲染表现层 (主线程)          │  Three.js 3D场景、粒子、特效
│  ← 双缓冲+预测插值获取位置 →         │
├─────────────────────────────────────┤
│          物理仿真层 (Web Worker)     │  动力学引擎、碰撞、演化、快照
├─────────────────────────────────────┤
│          基础内核层                  │  数学库、矢量运算、随机生成器
└─────────────────────────────────────┘
```

**关键架构规则速记：**
- 物理计算与渲染完全解耦，通过结构化消息通信。
- 物理 Worker 维护**双缓冲**（一计算、一传输），渲染线程取最新缓冲做**一阶外推插值**，消除通信延迟。
- 撤销/重做基于**完整系统快照**（最多 20 步），含所有天体状态与随机种子。
- 所有半透明对象按**显式排序管线**渲染。

**总周期：** 20 周 · **团队假设：** 1~2 名全栈 + 1 名技术美术。

---

## 2. 阶段零：工程脚手架与基础设施（动工前置）✅ 已完成（2026-06-20）

**目标：** 搭好可运行的五层空骨架，跑通「Vue UI ＋ Three.js 画布 ＋ 物理 Worker」三者并存的最小链路。

**实际环境：** Node v22.15.0 / npm 10.9.2 · Vite 8 · Vue 3.5 · Three.js 0.184 · TypeScript 6 · vue-tsc 3。

**任务清单：**
- [x] Vite 工程初始化（手动构建工程文件，非交互式模板）。
- [x] 安装依赖：
  - 核心：`three`、`vue`
  - 开发：`typescript`、`vite`、`@vitejs/plugin-vue`、`vue-tsc`、`@types/three`、`@types/node`、`vite-plugin-glsl`
  - （`eslint` / `prettier` 留待后续按需接入）
- [x] 创建五层目录骨架（对齐 `CLAUDE.md` 预期结构）：
  - `src/core/`（数学、向量、随机）
  - `src/physics/`（Web Worker 物理引擎）
  - `src/renderer/`（Three.js 渲染、材质、特效）
  - `src/gameplay/`（沙盒玩法、成就、挑战、编辑器）
  - `src/ui/`（Vue 3 界面组件）
  - `src/store/`（撤销栈、设置状态）
  - `src/utils/`（存档、截图、科普数据）
  - `public/`
- [x] 配置 `tsconfig.json`（`strict`、`verbatimModuleSyntax`、路径别名 `@/` → `src/`）＋ `tsconfig.node.json`。
- [x] 配置 `vite.config.ts`（`worker.format: 'es'`、GLSL 插件、`base: './'` 兼容子路径与离线）。
- [x] 搭建 `index.html` ＋ 入口 `src/main.ts`（挂载 Vue 根 `App.vue` ＋ 初始化 Three.js 渲染容器）。
- [x] 约定 Web Worker 通信协议骨架：
  - `src/physics/worker.ts` Worker 入口（消息分发 ＋ 主循环框架）
  - 主线程↔Worker 结构化消息类型：`PhysicsCommand`（指令）/ `PhysicsSnapshot`（快照）/ `PhysicsPong`
  - 双缓冲数据布局：位置/速度用 `Float32Array`，一算一传
  - `src/physics/bridge.ts` 主线程侧桥接封装
- [x] 基础内核层占位：`core/vector3.ts`（Vec3 就地/纯运算双接口）、`core/math.ts`、`core/prng.ts`（带种子、状态可读写，供快照复现）。
- [x] 构建/部署基础：`base: './'` ＋ `npm run build` 产物校验通过；CI 自动发布留待阶段四。

**交付物：** 可 `npm run dev` 启动的工程，Vue HUD ＋ Three.js 深空画布共存，物理 Worker 经 `ping → pong` 验证连通（HUD 显示「物理 Worker 已连通 ✓」）。

**验收标准（全部达成）：** `vue-tsc --noEmit` 类型检查零错误；`vite build` 成功且 Worker 独立打包（`dist/assets/worker-*.js`）；`npm run dev` 启动无报错，`/` 与 `/src/main.ts` 均返回 HTTP 200；五层目录与命名约定就位。

---

## 3. 阶段一：核心原型验证 ✅ 已完成（2026-06-20）

**目标：** 单恒星系统 Demo，跑通「物理 → 渲染」主链路与感知时间系统。

**单位制：** 天文单位制（AU · 年 · 太阳质量），G = 4π²，地球轨道周期恰为 1 年。

**任务清单：**

**物理内核**
- [x] velocity Verlet 全 N 体积分器（`physics/integrator.ts` ＋ `constants.ts`），恒星 + 4 行星圆轨道。
- [x] 感知时间系统（`core/time.ts`）：1× = 30 秒/年；档位 暂停/0.1/1/10/100/1000/10000，映射真实步长，隐藏真实倍率。
- [x] 物理 Worker accumulator 主循环（`physics/worker.ts`）：固定步长积分、高速降精度(细节省略)、快照 Transferable 零拷贝回传。

**渲染表现**
- [x] 主线程渲染循环：基于「最新快照 + 速度」做**一阶外推插值**（`SimulationController.onFrame`），消除通信延迟与帧率差。
- [x] 自由相机 OrbitControls（轨道/平移/缩放 + 阻尼）。
- [x] 岩质行星材质：程序化 fBm 噪声基底 map + bumpMap，受恒星点光源照亮（`proceduralTexture.ts` / `CelestialFactory.ts`）。〔预制特征遮罩留待阶段三〕
- [x] 恒星自发光球 + 加性光晕 sprite + 点光源；程序化星空点云背景。
- [x] 行星轨迹线（环形缓冲，增强「运行」观感）。

**玩法 / UI**
- [x] 调速面板（`ui/ControlPanel.vue`）：档位/暂停切换、模拟年数、感知速率描述、细节省略徽标、FPS、Worker 连通指示。

**交付物：** 单恒星系统在感知时间下稳定公转，自由视角观测，岩质行星受光可见，调速实时生效。

**验收结果（全部达成）：**
- `vue-tsc --noEmit` 零错误；`vite build` 成功（30 模块，worker 独立打包）。
- 物理正确性（独立脚本核验）：10 年模拟**能量漂移 0.000000%**（远优于标准模式 <0.01% 目标），轨道半径波动 0.0142%，轨道近乎完美闭合。
- `npm run dev` 启动无报错；首页与全部关键模块（含 `.vue`、worker、控制器、渲染工厂）transform 均返回 HTTP 200。
- 物理与渲染已彻底分离（Worker ↔ 主线程，结构化消息 + Transferable）。

---

## 4. 阶段二：可玩沙盒核心 ✅ 已完成（2026-06-20）

**目标：** N 体、碰撞、天体编辑器、撤销/重做、轨道预测，形成可玩沙盒。

**关键架构升级：** 固定天体集合 → 动态集合（稳定 id 体系）。渲染端按 id 增量同步 mesh，`BodiesMessage` 管增删、`SnapshotMessage` 更新位置。

**任务清单：**

**物理**
- [x] 全 N 体引力 + 两档精度（`world.ts` / `integrator.ts`）：
  - 流畅模式（默认）：半隐式（辛）欧拉，支撑更多天体；
  - 标准模式：velocity Verlet，高精度（能量漂移 0%，成就判定基准）。
- [x] 高速（>1000×）自动放大步长 + 限制步数 +「细节省略」提示（`worker.ts`）。
- [x] 碰撞三级判定（`collision.ts` / `world.handleCollisions`）：合并(<0.5)、撕裂(0.5~1.5)、毁灭(>1.5)，以接触面逃逸速度为基准；材质影响（气态易合并、恒星高速→超新星）；洛希极限函数已实现。〔独立潮汐撕裂/碎片场留待阶段三〕
- [x] 轨道预测（`predictTrajectory`：状态副本前瞻积分，回传选中天体未来轨迹，渲染为虚线）。

**玩法 / 状态**
- [x] 天体编辑器（`EditorPanel.vue`）：点击拾取选中，实时编辑 类型/质量/半径/颜色/速度，删除天体。
- [x] 预设场景：单恒星 / 双星 / 三星编舞（`presets.ts`），下拉即时加载。
- [x] 撤销/重做（`worker.ts` 内聚历史栈）：基于完整 `WorldState`（全部天体 + nextId + PRNG 状态，上限 20），回滚整盘。

**架构**
- [x] 物理/渲染彻底分离定型：双向结构化消息（load/edit/undo/predict ↔ bodies/snapshot/collision/prediction），快照 Transferable 零拷贝。

**交付物：** 可增删/编辑天体、切换精度档、观察三级碰撞与轨道预测、撤销/重做整盘、加载多预设的沙盒。

**验收结果（全部达成）：**
- `vue-tsc --noEmit` 零错误；`vite build` 成功（37 模块，worker 9.15kB 含全部物理逻辑）。
- 物理正确性（独立脚本核验）：流畅档欧拉 5 年半径波动 1.72%（稳定）；碰撞合并**动量零误差**、质量与体积守恒；三星编舞 8 年半径 [0.799, 0.801]（预设速度公式正确）。
- `npm run dev` 启动无报错；首页与全部关键模块（含 worker/world/Editor/Toolbar）transform 均 HTTP 200。

---

## 5. 阶段三：视觉与玩法打磨 ✅ 已完成（2026-06-20）

**目标：** 全天体视觉效果、合并后处理、透明排序、成就/挑战，性能达标 60fps。

**新增天体类型：** 黑洞（视界黑球 + 吸积盘环 + 引力透镜，接触即吞噬），打通碰撞/工厂/编辑器/预设全链路。

**任务清单：**

**渲染 / 后处理**
- [x] 全天体视觉：恒星米粒纹理 + 光晕、岩质 fBm 凹凸、气态纬向条带（`generateGasTexture`）、黑洞吸积盘（`generateAccretionTexture`）；各天体大气光晕。
- [x] 引力透镜（`updateLens` + `post.frag`）：取最近黑洞，相机视线夹角 <15° 全强度全屏偏折，15°~45° 线性衰减，屏幕空间 UV 拉拽畸变。
- [x] **合并后处理单通道 Shader**（`PostProcessor.ts` + `post.frag`）：引力透镜 + 阈值 bloom + ACES 色调映射 + 暗角，一次全屏绘制，替代多 Pass。
- [x] 显式透明排序管线（`renderLayers.ts`）：星空(0)→轨道线(10)→范围球(20)→大气光晕(30)→粒子(40)→UI 光效(50)，所有半透明对象显式 `renderOrder`。

**玩法**
- [x] 成就/挑战系统（`challenges.ts`）：3 个精确目标挑战，实时进度条 + 中断原因（`ChallengePanel.vue`），均要求标准模式判定。
- [x] 挑战「宜居双星」：恰好 3 天体 + 2 恒星 + 1 行星位于双星质心宜居带（`habitableZone.ts` 质光关系），选中恒星显示宜居带环（`HabitableRing.ts`）。
- [x] 引力弹弓能量计（`energyMeter.ts`）：追踪选中天体速率/峰值/相对基线增益，弹弓加速实时提示。

**性能**
- [x] 三档画质（`store/settings.ts`）：低/中/高，调节像素比、bloom、星空密度、后处理开关；低档关后处理保移动端帧率。

**交付物：** 含黑洞与引力透镜、影视级后处理、成就/挑战、能量计、画质分档的完整沙盒。

**验收结果（全部达成）：**
- `vue-tsc --noEmit` 零错误；`vite build` 成功（49 模块，含 GLSL shader 编译）。
- 逻辑正确性（独立脚本核验）：宜居带质光关系（太阳标定 [0.95,1.37]、亮星更远、暗星更近）；弹弓增益符号（加速+30%、减速−20%）；引力透镜夹角衰减（<15° 全强度、45° 截止、中间过渡）全部正确。
- `npm run dev` 启动无报错；首页与全部关键模块（含 `post.frag` GLSL、PostProcessor、challenges、ChallengePanel）transform 均 HTTP 200。

**说明 / 顺延：** 60fps 实测需浏览器人工核验（已提供画质分档保障）；碰撞碎片场、屏幕空间扭曲贴花（非黑洞局部畸变的贴花变体）等更重的视觉留作后续打磨。

---

## 6. 阶段四：内容填充与发布 ✅ 已完成（2026-06-20）

**目标：** 新手引导、科普内容、UI 动画、存档完善、截图工具、移动端适配，发布 V1.0。

**任务清单：**
- [x] 新手引导（`Onboarding.vue`）：5 步分步引导，首次访问 LocalStorage 标记，可跳过 / 重看。
- [x] 科普卡（`utils/scienceData.ts` + `ScienceCard.vue`）：恒星/岩质/气态/黑洞科普，选中天体折叠展示。
- [x] UI 动画：面板 transition 过渡（引导淡入、菜单滑入、科普卡展开）。
- [x] 存档/读档（`utils/storage.ts`）：LocalStorage 多槽位 + File API 导出/导入 .json（`fileIo.ts`），由最新快照重建 `WorldState`。
- [x] 截图工具（`SceneRenderer.capture` + `fileIo.downloadDataUrl`）：渲染后同步抓取，PNG 下载。
- [x] 移动端适配（`utils/device.ts` + `style.css` 媒体查询）：移动端默认低画质、`touch-action: none` 触控、响应式面板布局。
- [x] 发布流程：代码分割（three/vue/app 独立 chunk）、`base: './'` 相对路径、`README.md`、构建产物校验。

**交付物：** V1.0 正式版 —— 含存档/导出/截图/引导/科普/移动适配的完整可发布应用。

**验收结果（全部达成）：**
- `vue-tsc --noEmit` 零错误；`vite build` 成功（62 模块）。**代码分割生效**：three 548kB / vue 69kB / **应用仅 44kB（16.9kB gzip）**。
- 逻辑正确性（独立脚本核验）：存档序列化往返完全一致、缺字段归一化、非法存档抛错、截图文件名时间戳格式正确。
- `dist/index.html` 资源引用为相对路径（`./assets/...`），离线 / 子路径部署就绪。
- `npm run dev` 启动无报错；首页与全部新模块（storage/fileIo/scienceData/device/Onboarding/SaveMenu/ScienceCard）transform 均 HTTP 200。

**说明：** 移动端 30fps 与桌面 60fps 帧率需浏览器实机核验（代码层已提供画质分档与移动端降档保障）。

---

## 7. 横切关注点（贯穿全程的硬约束）

每阶段自查，任一违背需回退讨论：
- [ ] 主线程不承载重计算（重计算一律入 Worker）。
- [ ] 时间相关逻辑一律映射感知时间系统。
- [ ] 半透明对象一律走显式排序管线，不可省略。
- [ ] 视觉优先混合纹理策略，不承诺纯程序化（气态 / 恒星除外）。
- [ ] 物理精度提升属 V2.0 范围，V1.0 不扩张。
- [ ] 所有修改保证向下兼容，不随意改动原有正常逻辑。

---

## 8. 里程碑甘特概览

| 阶段 | 周次 | 核心交付物 | 关键验收 |
|------|------|-----------|----------|
| 阶段零 | W0 | 五层空骨架可运行 | 启动无报错、目录就位 |
| 阶段一 | W1–W4 | 单恒星系统 Demo | 感知时间轨道稳定、物理/渲染分离 |
| 阶段二 | W5–W9 | 可玩沙盒核心 | 200+/80+ 天体、快照回滚、碰撞判定 |
| 阶段三 | W10–W15 | 视觉与玩法打磨 | 桌面 60fps、透明排序、成就/挑战 |
| 阶段四 | W16–W20 | V1.0 发布 | 引导/存档/截图、移动端 30fps、离线可运行 |

---

## 9. V2.0 规划（占位，不在 V1.0 范围）

- 天体演化
- 航天器与探测器
- 高精度实验模块
- 在线分享

> 以上方向仅作前瞻记录，V1.0 周期内不实现；如需提前讨论须显式确认。
